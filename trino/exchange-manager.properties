# Exchange manager para Fault-Tolerant Execution (FTE).
# Dados intermediários dos estágios (joins/agg) são spooled em disco,
# permitindo queries com builds maiores que a RAM (substituto do spill legado).
exchange-manager.name=filesystem
exchange.base-directories=/tmp/trino-exchange
